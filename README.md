# Convolutional-neural-network-1
Convolutional Neural Network for Digit Recognition. This is my first CNN project, built to learn the fundamentals of deep learning and computer vision. The model is trained to recognize digits in images and correctly predict the number shown (e.g., predicting 2 when the image displays 2).
